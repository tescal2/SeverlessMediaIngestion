{
  "name": "elastictranscoder-createpipeline-lambda",
  "version": "1.0.0",
  "description": "Lambda function to create a CloudFormation custom resource for an Elastic Transcoder pipeline",
  "main": "index.js",
  "dependencies": {
    "async": "^1.5.2"
  },
  "devDependencies": {
    "aws-sdk": "^2.3.12"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
    "license": "Apache-2.0"
}
